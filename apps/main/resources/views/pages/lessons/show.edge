@layout({
title: article.title,
meta: {
url: '',
canonical: '',
}
})



{{-- {{inspect(article)}} --}}
@section({id: 'content', class:'show'})

@!heading({title: article.title, size: "xs", subtitle: article.summary})

<div>{{
    html.safe(
    article.content
    )
    }}</div>
@end

@end